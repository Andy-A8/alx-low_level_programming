#include "main.h"
#include <stdio.h>

/**
 * _strspn - The length of a prefix substring
 * @s: The string
 * @accept: The word
 *
 * Return: Nothing.
 */
unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
